// Root page — middleware (next-intl) handles locale detection and redirect.
// This file is required by Next.js but should never be rendered directly.
export default function RootPage() {
  return null;
}
